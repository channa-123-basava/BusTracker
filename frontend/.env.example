# Leave these unset for local development. Vite proxies /api and /socket.io
# to the backend running at http://localhost:5000.
# For production, set both values to your deployed backend URL.
# VITE_API_URL=https://your-api.example.com/api
# VITE_SOCKET_URL=https://your-api.example.com
